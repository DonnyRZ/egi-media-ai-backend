# EGI Media AI Backend workspace

Workspace foundation only. Business API routes and pipeline workers are intentionally not implemented yet.

## Swagger UI

After dependencies are installed, start the workspace with:

```bash
npm start
```

Open `http://localhost:5003/api-docs`.

The UI reads the planned contract from `swagger_output.json`. The documented endpoints are not runtime implementations at this stage.
