const { randomUUID } = require("node:crypto");
class InMemoryAccessAuditStore { constructor({ now = Date.now } = {}) { this.events = []; this.now = now; } async record(input) { const event = { id: input.id || randomUUID(), actorId: input.actorId || null, actorType: input.actorType || "unknown", tenantId: input.tenantId || null, companyId: input.companyId || null, action: input.action, outcome: input.outcome, requestId: input.requestId || null, metadata: input.metadata || {}, createdAt: new Date(this.now()).toISOString() }; this.events.push(event); return { ...event }; } async list({ tenantId, companyId, limit = 100 } = {}) { return this.events.filter((item) => (!tenantId || item.tenantId === tenantId) && (!companyId || item.companyId === companyId)).slice(-limit).reverse(); } }
class PostgresAccessAuditStore { constructor({ db } = {}) { if (!db?.query) throw new TypeError("PostgresAccessAuditStore requires db"); this.db = db; } async record(input) { const result = await this.db.query("INSERT INTO ai.access_audit_events (id,actor_id,actor_type,tenant_id,company_id,action,outcome,request_id,metadata_jsonb) VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9::jsonb) RETURNING id,created_at", [input.id || randomUUID(), input.actorId || null, input.actorType || "unknown", input.tenantId || null, input.companyId || null, input.action, input.outcome, input.requestId || null, JSON.stringify(input.metadata || {})]); return result.rows[0]; } }
module.exports = { InMemoryAccessAuditStore, PostgresAccessAuditStore };
