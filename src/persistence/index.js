const { PostgresRecordStore } = require("./postgres-record-store");
const stores = require("./postgres-stores");
const { PostgresIssueStore } = require("./postgres-issue-store");
const stageStores = require("./postgres-stage-stores");
const contextStores = require("./postgres-context-stores");
const { PostgresJobStore } = require("./postgres-job-store");
const { PostgresEmailDeliveryStore } = require("./postgres-delivery-store");
const ingestStores = require("./postgres-ingest-stores");
function createPostgresPersistence({ db } = {}) { if (!db) throw new TypeError("PostgreSQL persistence requires the AI database adapter"); return { snapshotStore:new ingestStores.PostgresSourceSnapshotStore({db}), watermarkStore:new ingestStores.PostgresWatermarkStore({db}), jobStore:new PostgresJobStore({db}), deliveryStore:new PostgresEmailDeliveryStore({db}), contextDraftStore:new contextStores.PostgresCompanyContextDraftStore({db}), effectiveContextStore:new contextStores.PostgresEffectiveCompanyContextStore({db}), issueStore:new PostgresIssueStore({db}), relevanceDecisionStore:new stores.PostgresRelevanceDecisionStore({db}), matchDecisionStore:new stores.PostgresIssueMatchDecisionStore({db}), analysisStore:new stores.PostgresIssueAnalysisStore({db}), priorityStore:new stores.PostgresIssuePriorityStore({db}), rationaleStore:new stageStores.PostgresRelevanceRationaleStore({db}), labelStore:new stageStores.PostgresClaimLabelStore({db}), reasonStore:new stageStores.PostgresPriorityReasonStore({db}), blurbStore:new stageStores.PostgresDirectAlertBlurbStore({db}), savedIssueStore:new stores.PostgresSavedIssueStore({db}), feedbackStore:new stores.PostgresFeedbackStore({db}), alertEventStore:new stores.PostgresAlertEventStore({db}), alertPreferenceStore:new stores.PostgresAlertPreferenceStore({db}), reportDraftStore:new stores.PostgresReportDraftStore({db}), reportNarrativeStore:new stores.PostgresReportNarrativeStore({db}) }; }
module.exports = { PostgresRecordStore, PostgresIssueStore, PostgresJobStore, PostgresEmailDeliveryStore, createPostgresPersistence, ...stores, ...stageStores, ...contextStores, ...ingestStores };
